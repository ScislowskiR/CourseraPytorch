/*

#include "convertCase.h"
char Convertion::convertCase(char text[]){
    int len = sizeof(text)/sizeof(text[0]);
    for(int i = 0; i<len; i++){
        if(isupper(text[i])){
            text[i] = tolower(text[i]);
        }
        else if(islower(text[i])){
            text[i] = toupper(text[i]);
        }
        else{
        }
    }
    cout<<text;
}
int main()
{
    char txt[]="dEfeDeEDEggHY";
    Convertion abc;
    abc.convertCase(txt);
    return 0;
}
*/
