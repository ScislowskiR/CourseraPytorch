//
// Created by scisl on 4/4/2021.
//

#ifndef CLION_TABUTILS_H
#define CLION_TABUTILS_H

#endif //CLION_TABUTILS_H

#pragma once
#include <iostream>
#include <chrono>
#include <bits/stdc++.h>
#include <algorithm>
#include <ctime>
#include <stdlib.h>
using namespace std;
using namespace std::chrono;
/*int tab[5];// = {1,2,3,4,5};
int *wsk = &tab[3];
//int size=14;
int random(int min, int max) //range : [min, max]
{
    static bool first = true;
    if (first)
    {
        srand( time(NULL) ); //seeding for the first time only!
        first = false;
    }
    return min + rand() % (( max + 1 ) - min);
}
void printTab (int *tab,unsigned int size)
{
    cout<<random(0,size);
    int length=size;
    for (int i=0;i<length;i++){
        cout<<i;
    }
    cout<<endl;
    for (int i=0;i<=int(size);i++){
        cout<<tab[i]<<" ";
    }
}
int tablica[]={1,2,3,4,5};
void reverseTab(int*tab, unsigned int size){
    *tab=tab[3];
    cout<<tab<<endl;
}*/
/*
void reverseTab(int * tab, unsigned int size){
    reverse(tab, tab + 14);
    for (int i = 0; i < int(size); i++)
        cout << tab[i] << " ";
    cout<<endl<<size<<endl;
}*/
