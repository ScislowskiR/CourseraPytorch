#pragma once
#include <algorithm>
#include <iostream>
using namespace std;
class Resize {public:
    char resizeArray(char *array, unsigned int newSize);
};

char * resizeArray(char * array, unsigned int newSize);